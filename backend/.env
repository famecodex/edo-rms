# Server Configuration
NODE_ENV=development
PORT=5001

# Database Configuration
MONGODB_URI=mongodb+srv://favourorobor:faAppzz1@axiaapp.qtpcutd.mongodb.net/?retryWrites=true&w=majority&appName=AxiaApp
MONGO_URI=mongodb+srv://favourorobor:faAppzz1@axiaapp.qtpcutd.mongodb.net/?retryWrites=true&w=majority&appName=AxiaApp
MONGODB_URI_TEST=mongodb+srv://favourorobor:faAppzz1@axiaapp.qtpcutd.mongodb.net/?retryWrites=true&w=majority&appName=AxiaApp

# JWT Configuration
JWT_SECRET=super-secret-1CHANGE-ME2480
JWT_EXPIRY=30d

# Cookie Configuration
COOKIE_SECRET=3d0-rm5-c00k13-53cr3t-2025
COOKIE_EXPIRY=30d

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100
RATE_LIMIT_MESSAGE="Too many requests from this IP, please try again later"

# Security
PASSWORD_SALT_ROUNDS=10
SESSION_SECRET=3d0-rm5-53ss10n-53cr3t-2025
MIN_PASSWORD_LENGTH=8
PASSWORD_PATTERN="^(?=.*[a-z])(?=.*[A-Z])(?=.*\\d)[a-zA-Z\\d@$!%*?&]{8,}$"

# Logging
LOG_LEVEL=debug
LOG_FORMAT=dev

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
ALLOWED_HEADERS=Content-Type,Authorization

# Email Domains
MINISTRY_EMAIL_DOMAIN=@edo.gov.ng
# Minimal backend .env — only the keys this app needs (plus a few common optional ones).
#
# Keep secrets private. Do NOT commit this file to source control. Use environment
# variables in production or a secrets manager.

# Core runtime
NODE_ENV=development
PORT=5001

# Database (required) — your provided MongoDB connection string
# Example (Atlas): mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/edo-rms?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://favourorobor:faAppzz1@axiaapp.qtpcutd.mongodb.net/?retryWrites=true&w=majority&appName=AxiaApp

# Optional test DB (used when NODE_ENV=test)
MONGODB_URI_TEST=mongodb://localhost:27017/edo-rms-test

# JWT secret (required) — you provided this value
JWT_SECRET=super-secret-1CHANGE-ME2480
JWT_EXPIRY=30d

# CORS origin(s) used by the server (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend URL used in password reset links (optional but recommended)
FRONTEND_URL=http://localhost:5173

# SMTP settings used for sending emails (optional)
# Uncomment and set if you want email features to work
#SMTP_HOST=smtp.gmail.com
#SMTP_PORT=587
#SMTP_USER=youremail@example.com
#SMTP_PASS=your-email-password-or-app-password

# Test API base for integration tests (optional)
TEST_API_URL=http://localhost:5001

# If you need additional variables (COOKIE_SECRET, SESSION_SECRET, etc.)
# add them here — they are optional for basic auth but recommended for production.

# End of file
